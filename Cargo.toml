[package]
name = "semgrep"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
